import { SugarLevelFormatPipe } from './sugar-level-format.pipe';

describe('SugarLevelFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SugarLevelFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
