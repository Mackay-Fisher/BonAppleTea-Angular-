import { FormatCamelCasePipe } from './format-camel-case.pipe';

describe('FormatCamelCasePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCamelCasePipe();
    expect(pipe).toBeTruthy();
  });
});
