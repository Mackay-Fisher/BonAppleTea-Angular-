import { IceLevelReformatPipe } from './ice-level-reformat.pipe';

describe('IceLevelReformatPipe', () => {
  it('create an instance', () => {
    const pipe = new IceLevelReformatPipe();
    expect(pipe).toBeTruthy();
  });
});
